<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.likhitasuresh.miniproject1">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Password" android:clearTaskOnLaunch="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".info" />
        <activity android:name=".settings" />
        <activity android:name=".home1" />
        <activity android:name=".about" />
        <activity android:name=".Home" />
        <activity
            android:name=".Trial"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".Tab11Fragment" />
        <activity android:name=".Tab12Fragment" />
        <activity android:name=".Tab13Fragment"></activity>
    </application>

</manifest>